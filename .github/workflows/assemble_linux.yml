name: 🐧 Assemble Linux
on:
  workflow_call:
    inputs:
      godot-jvm-version:
        type: string

concurrency:
  group: ci-${{github.actor}}-${{github.head_ref || github.run_number}}-${{github.ref}}-linux_assemble
  cancel-in-progress: true

jobs:
  create-linux-library-zip:
    runs-on: ubuntu-latest
    name: ${{ matrix.name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Create release library zip
            target: release
            arch: x86_64

          - name: Create debug library zip
            target: debug
            arch: x86_64
    steps:
      - name: Download ${{ matrix.target }} library
        uses: actions/download-artifact@v7
        with:
          name: ${{ matrix.target }}_linux_${{ matrix.arch }}
          path: godot-jvm-gdextension-linuxbsd-${{ matrix.arch }}-${{ matrix.target }}-${{ inputs.godot-jvm-version }}

      - name: Assemble Linux library
        run: |
          mkdir -p package/addons
          cp -R godot-jvm-gdextension-linuxbsd-${{ matrix.arch }}-${{ matrix.target }}-${{ inputs.godot-jvm-version }}/jvm package/addons/jvm
        shell: bash

      # Upload the raw "addons" folder rather than a pre-made zip: actions/upload-artifact always
      # wraps whatever it's given in its own zip, so uploading a zip here results in a zip nested
      # inside that zip once a human downloads the artifact. The actual release-asset zip gets made
      # once, at the point deploy_godot.yml attaches it to the GitHub release.
      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: godot-jvm-gdextension-linuxbsd-${{ matrix.arch }}-${{ matrix.target }}-${{ inputs.godot-jvm-version }}
          path: package
